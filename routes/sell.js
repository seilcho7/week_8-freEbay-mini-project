const express = require('express');

const Router = express.Router;

const sellRoutes = Router();

// const {
//     sellPage
// } = require('../controllers/sell');

// sellRoutes.get('/', sellPage);

module.exports = sellRoutes;